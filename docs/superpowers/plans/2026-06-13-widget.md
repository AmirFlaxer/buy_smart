# Widget למסך הבית - Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Widget למסך הבית של Android שמציג את פריטי הרשימה הפעילה (שטרם נקנו) ומאפשר הוספה מהירה דרך דיאלוג צף.

**Architecture:** ה-widget בנוי ב-Glance (Compose for App Widgets). הוא קורא נתונים מ-Room cache המקומי (שכבר מתעדכן בכל snapshot מ-Firestore) - בלי auth או רשת. ה-listId הפעיל נקרא מ-DataStore. הוספה מהירה מתבצעת דרך Activity שקופה (`@AndroidEntryPoint`). ה-widget מתעדכן מתוך `ItemRepositoryImpl` בכל שינוי נתונים.

**Tech Stack:** Kotlin, Glance 1.1.1, Hilt, Room, DataStore, Coroutines.

**מקור:** מסמך עיצוב ב-`docs/superpowers/specs/2026-06-13-widget-design.md`.

**הערת build (Windows):** לפני כל פקודת gradle, הרץ ב-Bash tool:
`export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr"`
ואז `cd c:/Projects/buy_smart`. ה-Bash tool הוא bash ולא PowerShell, ו-JAVA_HOME לא מוגדר ב-PATH שלו.

---

## File Structure

| קובץ | אחריות |
|------|--------|
| `gradle/libs.versions.toml` | הוספת גרסה + library של Glance |
| `app/build.gradle.kts` | הוספת dependency של Glance |
| `app/src/main/java/com/amir/buysmart/presentation/widget/WidgetItems.kt` | פונקציה טהורה לסינון/מיון/חיתוך פריטי ה-widget |
| `app/src/test/java/com/amir/buysmart/presentation/widget/WidgetItemsTest.kt` | unit test לפונקציה הטהורה |
| `app/src/main/java/com/amir/buysmart/presentation/widget/WidgetEntryPoint.kt` | Hilt EntryPoint שחושף ItemDao + DataStore ל-Glance |
| `app/src/main/java/com/amir/buysmart/presentation/widget/WidgetUpdater.kt` | object עם suspend fun update(context) |
| `app/src/main/java/com/amir/buysmart/presentation/widget/BuySmartWidget.kt` | GlanceAppWidget - ה-UI של ה-widget |
| `app/src/main/java/com/amir/buysmart/presentation/widget/BuySmartWidgetReceiver.kt` | GlanceAppWidgetReceiver |
| `app/src/main/java/com/amir/buysmart/presentation/widget/QuickAddActivity.kt` | Activity שקופה - דיאלוג הוספה מהירה |
| `app/src/main/res/xml/buysmart_widget_info.xml` | מטא-דאטה של ה-widget |
| `app/src/main/res/values/strings.xml` | מחרוזות חדשות (modify) |
| `app/src/main/res/values/themes.xml` | theme שקוף ל-QuickAddActivity (modify/create) |
| `app/src/main/AndroidManifest.xml` | רישום receiver + activity (modify) |
| `app/src/main/java/com/amir/buysmart/data/repository/ItemRepositoryImpl.kt` | hook לעדכון ה-widget (modify) |

---

## Task 1: הוספת dependency של Glance

**Files:**
- Modify: `gradle/libs.versions.toml`
- Modify: `app/build.gradle.kts`

- [ ] **Step 1: הוסף גרסה ו-library ל-`libs.versions.toml`**

תחת `[versions]` הוסף שורה (אחרי `coil = "2.7.0"`):

```toml
glance = "1.1.1"
```

תחת `[libraries]` הוסף שורה (אחרי שורת `coil-compose`):

```toml
glance-appwidget = { group = "androidx.glance", name = "glance-appwidget", version.ref = "glance" }
```

- [ ] **Step 2: הוסף את ה-dependency ל-`app/build.gradle.kts`**

ב-block של `dependencies`, אחרי השורה `implementation(libs.coil.compose)`:

```kotlin
implementation(libs.glance.appwidget)
```

- [ ] **Step 3: ודא שה-Gradle sync עובר**

Run: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && cd c:/Projects/buy_smart && ./gradlew.bat :app:dependencies --configuration debugRuntimeClasspath -q | grep glance`
Expected: מופיע `androidx.glance:glance-appwidget:1.1.1` בפלט.

- [ ] **Step 4: Commit**

```bash
git add gradle/libs.versions.toml app/build.gradle.kts
git commit -m "build: הוספת dependency של Glance ל-widget"
```

---

## Task 2: פונקציה טהורה `widgetItems` (TDD)

הפונקציה מסננת פריטים שטרם נקנו ולא ממתינים לחידוש, ממיינת לפי priority, וחותכת לכמות מקסימלית. זו הלוגיקה היחידה הניתנת ל-unit test אמיתי, ולכן נבנית ב-TDD תחילה.

**Files:**
- Create: `app/src/main/java/com/amir/buysmart/presentation/widget/WidgetItems.kt`
- Test: `app/src/test/java/com/amir/buysmart/presentation/widget/WidgetItemsTest.kt`

- [ ] **Step 1: כתוב את הבדיקה הנכשלת**

צור `app/src/test/java/com/amir/buysmart/presentation/widget/WidgetItemsTest.kt`:

```kotlin
package com.amir.buysmart.presentation.widget

import com.amir.buysmart.domain.model.ItemPriority
import com.amir.buysmart.domain.model.ItemType
import com.amir.buysmart.domain.model.ShoppingItem
import com.amir.buysmart.domain.model.ShoppingLocation
import org.junit.Assert.assertEquals
import org.junit.Test

class WidgetItemsTest {

    private fun item(
        id: String,
        name: String,
        isBought: Boolean = false,
        pendingRefill: Boolean = false,
        priority: ItemPriority = ItemPriority.NORMAL
    ) = ShoppingItem(
        id = id,
        name = name,
        location = ShoppingLocation.SUPERMARKET,
        type = ItemType.RECURRING,
        isBought = isBought,
        addedBy = "u1",
        listId = "list1",
        priority = priority,
        pendingRefill = pendingRefill
    )

    @Test
    fun `filters out bought and pending refill items`() {
        val items = listOf(
            item("1", "חלב"),
            item("2", "לחם", isBought = true),
            item("3", "ביצים", pendingRefill = true)
        )
        val result = widgetItems(items, max = 12)
        assertEquals(listOf("חלב"), result.map { it.name })
    }

    @Test
    fun `sorts by priority - urgent first`() {
        val items = listOf(
            item("1", "רגיל", priority = ItemPriority.NORMAL),
            item("2", "דחוף", priority = ItemPriority.URGENT),
            item("3", "לא דחוף", priority = ItemPriority.NOT_URGENT)
        )
        val result = widgetItems(items, max = 12)
        assertEquals(listOf("דחוף", "רגיל", "לא דחוף"), result.map { it.name })
    }

    @Test
    fun `caps result to max`() {
        val items = (1..20).map { item(it.toString(), "פריט$it") }
        val result = widgetItems(items, max = 12)
        assertEquals(12, result.size)
    }

    @Test
    fun `returns empty list for empty input`() {
        assertEquals(emptyList<ShoppingItem>(), widgetItems(emptyList(), max = 12))
    }
}
```

- [ ] **Step 2: הרץ את הבדיקה כדי לוודא שהיא נכשלת**

Run: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && cd c:/Projects/buy_smart && ./gradlew.bat :app:testDebugUnitTest --tests "com.amir.buysmart.presentation.widget.WidgetItemsTest"`
Expected: FAIL - compilation error, `widgetItems` unresolved reference.

- [ ] **Step 3: כתוב את המימוש המינימלי**

צור `app/src/main/java/com/amir/buysmart/presentation/widget/WidgetItems.kt`:

```kotlin
package com.amir.buysmart.presentation.widget

import com.amir.buysmart.domain.model.ShoppingItem

/**
 * הפריטים שמוצגים ב-widget: רק מה שטרם נקנה ואינו ממתין לחידוש,
 * ממוין לפי דחיפות (URGENT ראשון), חתוך לכמות מקסימלית.
 */
fun widgetItems(items: List<ShoppingItem>, max: Int): List<ShoppingItem> =
    items
        .filter { !it.isBought && !it.pendingRefill }
        .sortedBy { it.priority.ordinal }
        .take(max)
```

- [ ] **Step 4: הרץ את הבדיקה כדי לוודא שהיא עוברת**

Run: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && cd c:/Projects/buy_smart && ./gradlew.bat :app:testDebugUnitTest --tests "com.amir.buysmart.presentation.widget.WidgetItemsTest"`
Expected: PASS - 4 tests passed.

- [ ] **Step 5: Commit**

```bash
git add app/src/main/java/com/amir/buysmart/presentation/widget/WidgetItems.kt app/src/test/java/com/amir/buysmart/presentation/widget/WidgetItemsTest.kt
git commit -m "feat: widgetItems - סינון ומיון פריטי ה-widget (+tests)"
```

---

## Task 3: Hilt EntryPoint לגישת נתונים מ-Glance

Glance לא תומך ב-`@Inject` ישיר, לכן ה-widget ניגש ל-`ItemDao` ול-`DataStore` דרך `@EntryPoint`.

**Files:**
- Create: `app/src/main/java/com/amir/buysmart/presentation/widget/WidgetEntryPoint.kt`

- [ ] **Step 1: צור את ה-EntryPoint**

```kotlin
package com.amir.buysmart.presentation.widget

import androidx.datastore.core.DataStore
import androidx.datastore.preferences.core.Preferences
import com.amir.buysmart.data.local.ItemDao
import dagger.hilt.EntryPoint
import dagger.hilt.InstallIn
import dagger.hilt.components.SingletonComponent

@EntryPoint
@InstallIn(SingletonComponent::class)
interface WidgetEntryPoint {
    fun itemDao(): ItemDao
    fun dataStore(): DataStore<Preferences>
}
```

- [ ] **Step 2: ודא קומפילציה**

Run: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && cd c:/Projects/buy_smart && ./gradlew.bat :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 3: Commit**

```bash
git add app/src/main/java/com/amir/buysmart/presentation/widget/WidgetEntryPoint.kt
git commit -m "feat: WidgetEntryPoint - גישת Hilt ל-DAO/DataStore מ-Glance"
```

---

## Task 4: `WidgetUpdater` - עדכון ה-widget

**Files:**
- Create: `app/src/main/java/com/amir/buysmart/presentation/widget/WidgetUpdater.kt`

- [ ] **Step 1: צור את ה-updater**

```kotlin
package com.amir.buysmart.presentation.widget

import android.content.Context

/** מרענן את כל מופעי ה-widget. נקרא אחרי שינוי נתונים. */
object WidgetUpdater {
    suspend fun update(context: Context) {
        BuySmartWidget().updateAll(context)
    }
}
```

הערה: `BuySmartWidget` נוצר ב-Task 5. אם מבצעים את המשימות לפי הסדר, קובץ זה לא יתקמפל עד שיושלם Task 5 - לכן ה-commit של משימה זו מתבצע בסוף Task 5. דלג כעת על build/commit.

- [ ] **Step 2: המשך ל-Task 5 (build משותף)**

---

## Task 5: `BuySmartWidget` - ה-UI של ה-widget

ה-widget מציג כותרת ("BuySmart"), כפתור +, ורשימת פריטים. קורא נתונים ב-`provideGlance` דרך ה-EntryPoint.

**Files:**
- Create: `app/src/main/java/com/amir/buysmart/presentation/widget/BuySmartWidget.kt`

- [ ] **Step 1: צור את ה-GlanceAppWidget**

```kotlin
package com.amir.buysmart.presentation.widget

import android.content.Context
import androidx.compose.runtime.Composable
import androidx.datastore.preferences.core.stringPreferencesKey
import androidx.glance.GlanceModifier
import androidx.glance.action.clickable
import androidx.glance.appwidget.GlanceAppWidget
import androidx.glance.appwidget.action.actionStartActivity
import androidx.glance.appwidget.cornerRadius
import androidx.glance.appwidget.lazy.LazyColumn
import androidx.glance.appwidget.lazy.items
import androidx.glance.appwidget.provideContent
import androidx.glance.background
import androidx.glance.color.ColorProvider
import androidx.glance.layout.Alignment
import androidx.glance.layout.Box
import androidx.glance.layout.Column
import androidx.glance.layout.Row
import androidx.glance.layout.Spacer
import androidx.glance.layout.fillMaxSize
import androidx.glance.layout.fillMaxWidth
import androidx.glance.layout.padding
import androidx.glance.layout.size
import androidx.glance.text.FontWeight
import androidx.glance.text.Text
import androidx.glance.text.TextStyle
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp
import com.amir.buysmart.domain.model.ShoppingItem
import com.amir.buysmart.presentation.MainActivity
import dagger.hilt.android.EntryPointAccessors
import kotlinx.coroutines.flow.first

private val Green = Color(0xFF2E7D32)
private val OnGreen = Color(0xFFFFFFFF)
private val Surface = Color(0xFFF7FBF1)
private val OnSurface = Color(0xFF191D17)
private const val MAX_ITEMS = 12
private val activeListKey = stringPreferencesKey("active_list_id")

class BuySmartWidget : GlanceAppWidget() {

    override suspend fun provideGlance(context: Context, id: androidx.glance.appwidget.GlanceId) {
        val entryPoint = EntryPointAccessors.fromApplication(
            context.applicationContext, WidgetEntryPoint::class.java
        )
        val listId = entryPoint.dataStore().data.first()[activeListKey]
        val items: List<ShoppingItem> = if (listId == null) {
            emptyList()
        } else {
            val entities = entryPoint.itemDao().getItemsForList(listId).first()
            widgetItems(entities.map { it.toDomain() }, MAX_ITEMS)
        }
        provideContent {
            WidgetContent(loggedOut = listId == null, items = items)
        }
    }

    @Composable
    private fun WidgetContent(loggedOut: Boolean, items: List<ShoppingItem>) {
        Column(
            modifier = GlanceModifier
                .fillMaxSize()
                .background(ColorProvider(Surface))
                .cornerRadius(16.dp)
                .padding(12.dp)
        ) {
            // כותרת + כפתור הוספה
            Row(
                modifier = GlanceModifier.fillMaxWidth(),
                verticalAlignment = Alignment.CenterVertically
            ) {
                Text(
                    text = "BuySmart",
                    style = TextStyle(
                        color = ColorProvider(Green),
                        fontSize = 18.sp,
                        fontWeight = FontWeight.Bold
                    ),
                    modifier = GlanceModifier.defaultWeight()
                        .clickable(actionStartActivity<MainActivity>())
                )
                Box(
                    modifier = GlanceModifier
                        .size(36.dp)
                        .cornerRadius(18.dp)
                        .background(ColorProvider(Green))
                        .clickable(actionStartActivity<QuickAddActivity>()),
                    contentAlignment = Alignment.Center
                ) {
                    Text("+", style = TextStyle(color = ColorProvider(OnGreen), fontSize = 22.sp, fontWeight = FontWeight.Bold))
                }
            }
            Spacer(modifier = GlanceModifier.size(8.dp))

            when {
                loggedOut -> CenterMessage("התחבר כדי לראות את הרשימה")
                items.isEmpty() -> CenterMessage("הרשימה ריקה 🎉")
                else -> LazyColumn(modifier = GlanceModifier.fillMaxSize()) {
                    items(items, itemId = { it.id.hashCode().toLong() }) { item ->
                        ItemRow(item)
                    }
                }
            }
        }
    }

    @Composable
    private fun ItemRow(item: ShoppingItem) {
        Row(
            modifier = GlanceModifier
                .fillMaxWidth()
                .padding(vertical = 6.dp)
                .clickable(actionStartActivity<MainActivity>()),
            verticalAlignment = Alignment.CenterVertically
        ) {
            Text(item.location.emoji, style = TextStyle(fontSize = 16.sp))
            Spacer(modifier = GlanceModifier.size(8.dp))
            Text(
                text = item.name + if (item.quantity.isNotBlank()) " (${item.quantity})" else "",
                style = TextStyle(color = ColorProvider(OnSurface), fontSize = 15.sp),
                modifier = GlanceModifier.defaultWeight()
            )
        }
    }

    @Composable
    private fun CenterMessage(text: String) {
        Box(
            modifier = GlanceModifier.fillMaxSize(),
            contentAlignment = Alignment.Center
        ) {
            Text(
                text = text,
                style = TextStyle(color = ColorProvider(OnSurface), fontSize = 15.sp),
                modifier = GlanceModifier.clickable(actionStartActivity<MainActivity>())
            )
        }
    }
}
```

הערה על imports: `sp` מגיע מ-`androidx.compose.ui.unit.sp`. הוסף `import androidx.compose.ui.unit.sp` לראש הקובץ.

- [ ] **Step 2: ודא קומפילציה (כולל WidgetUpdater מ-Task 4)**

Run: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && cd c:/Projects/buy_smart && ./gradlew.bat :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL. אם יש שגיאות import של Glance (שמות חבילה השתנו בין גרסאות), תקן לפי ההצעות של ה-compiler - ה-API הציבורי של `androidx.glance.appwidget` יציב בגרסה 1.1.1.

- [ ] **Step 3: Commit (BuySmartWidget + WidgetUpdater יחד)**

```bash
git add app/src/main/java/com/amir/buysmart/presentation/widget/BuySmartWidget.kt app/src/main/java/com/amir/buysmart/presentation/widget/WidgetUpdater.kt
git commit -m "feat: BuySmartWidget - UI של ה-widget + WidgetUpdater"
```

---

## Task 6: `BuySmartWidgetReceiver` + מטא-דאטה + רישום Manifest

**Files:**
- Create: `app/src/main/java/com/amir/buysmart/presentation/widget/BuySmartWidgetReceiver.kt`
- Create: `app/src/main/res/xml/buysmart_widget_info.xml`
- Modify: `app/src/main/AndroidManifest.xml`

- [ ] **Step 1: צור את ה-Receiver**

```kotlin
package com.amir.buysmart.presentation.widget

import androidx.glance.appwidget.GlanceAppWidget
import androidx.glance.appwidget.GlanceAppWidgetReceiver

class BuySmartWidgetReceiver : GlanceAppWidgetReceiver() {
    override val glanceAppWidget: GlanceAppWidget = BuySmartWidget()
}
```

- [ ] **Step 2: צור את קובץ המטא-דאטה**

צור `app/src/main/res/xml/buysmart_widget_info.xml`:

```xml
<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:targetCellWidth="3"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:previewImage="@mipmap/ic_launcher"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/glance_default_loading_layout" />
```

הערה: `@layout/glance_default_loading_layout` מסופק על ידי ספריית Glance עצמה - אין צורך ליצור אותו.

- [ ] **Step 3: רשום את ה-receiver ב-Manifest**

ב-`app/src/main/AndroidManifest.xml`, בתוך `<application>`, אחרי ה-`<provider>` של ה-FileProvider, הוסף:

```xml
<!-- Widget למסך הבית -->
<receiver
    android:name=".presentation.widget.BuySmartWidgetReceiver"
    android:exported="false">
    <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
    </intent-filter>
    <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/buysmart_widget_info" />
</receiver>
```

- [ ] **Step 4: ודא קומפילציה**

Run: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && cd c:/Projects/buy_smart && ./gradlew.bat :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 5: Commit**

```bash
git add app/src/main/java/com/amir/buysmart/presentation/widget/BuySmartWidgetReceiver.kt app/src/main/res/xml/buysmart_widget_info.xml app/src/main/AndroidManifest.xml
git commit -m "feat: רישום BuySmartWidgetReceiver + מטא-דאטה ב-Manifest"
```

---

## Task 7: `QuickAddActivity` - דיאלוג הוספה מהירה

Activity שקופה עם דיאלוג Compose: שדה שם + כפתור "הוסף". משתמשת ב-`@AndroidEntryPoint` עם `@Inject`. סיווג location מקומי בלבד (ללא רשת): `suggestLocation` → history → SUPERMARKET.

**Files:**
- Create: `app/src/main/java/com/amir/buysmart/presentation/widget/QuickAddActivity.kt`
- Modify: `app/src/main/res/values/themes.xml`
- Modify: `app/src/main/res/values/strings.xml`
- Modify: `app/src/main/AndroidManifest.xml`

- [ ] **Step 1: הוסף theme שקוף**

פתח את `app/src/main/res/values/themes.xml`. אם הקובץ לא קיים, צור אותו עם המעטפת `<resources> ... </resources>`. הוסף בתוך `<resources>`:

```xml
<style name="Theme.BuySmart.Transparent" parent="@android:style/Theme.Translucent.NoTitleBar">
    <item name="android:windowBackground">@android:color/transparent</item>
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:windowNoTitle">true</item>
</style>
```

- [ ] **Step 2: צור את ה-Activity**

```kotlin
package com.amir.buysmart.presentation.widget

import android.content.Intent
import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.fillMaxWidth
import androidx.compose.foundation.layout.padding
import androidx.compose.material3.AlertDialog
import androidx.compose.material3.Button
import androidx.compose.material3.OutlinedTextField
import androidx.compose.material3.Text
import androidx.compose.material3.TextButton
import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.setValue
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp
import com.amir.buysmart.domain.model.ItemNotePresets
import com.amir.buysmart.domain.model.ItemType
import com.amir.buysmart.domain.model.ShoppingItem
import com.amir.buysmart.domain.model.ShoppingLocation
import com.amir.buysmart.domain.repository.ItemRepository
import com.amir.buysmart.domain.usecase.AddItemUseCase
import com.amir.buysmart.presentation.MainActivity
import com.amir.buysmart.presentation.theme.BuySmartTheme
import com.google.firebase.auth.FirebaseAuth
import dagger.hilt.android.AndroidEntryPoint
import kotlinx.coroutines.flow.first
import kotlinx.coroutines.launch
import androidx.lifecycle.lifecycleScope
import javax.inject.Inject

@AndroidEntryPoint
class QuickAddActivity : ComponentActivity() {

    @Inject lateinit var addItemUseCase: AddItemUseCase
    @Inject lateinit var itemRepository: ItemRepository
    @Inject lateinit var auth: FirebaseAuth

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        // ללא משתמש מחובר - פותח את האפליקציה להתחברות
        val user = auth.currentUser
        if (user == null) {
            startActivity(Intent(this, MainActivity::class.java))
            finish()
            return
        }

        setContent {
            BuySmartTheme {
                var name by remember { mutableStateOf("") }
                AlertDialog(
                    onDismissRequest = { finish() },
                    title = { Text("הוספה מהירה") },
                    text = {
                        Column(modifier = Modifier.fillMaxWidth()) {
                            OutlinedTextField(
                                value = name,
                                onValueChange = { name = it },
                                label = { Text("שם הפריט") },
                                modifier = Modifier.fillMaxWidth().padding(top = 8.dp)
                            )
                        }
                    },
                    confirmButton = {
                        Button(onClick = { if (name.isNotBlank()) addAndFinish(name.trim(), user.uid, displayName(user)) }) {
                            Text("הוסף")
                        }
                    },
                    dismissButton = {
                        TextButton(onClick = { finish() }) { Text("ביטול") }
                    }
                )
            }
        }
    }

    private fun displayName(user: com.google.firebase.auth.FirebaseUser): String =
        user.displayName ?: user.email?.substringBefore("@") ?: ""

    private fun addAndFinish(name: String, userId: String, displayName: String) {
        lifecycleScope.launch {
            // ה-listId הפעיל נשמר ב-DataStore; ניגשים אליו דרך ה-EntryPoint (אותו מפתח כמו ListRepositoryImpl)
            val entryPoint = dagger.hilt.android.EntryPointAccessors.fromApplication(
                applicationContext, WidgetEntryPoint::class.java
            )
            val activeListId = entryPoint.dataStore().data.first()[
                androidx.datastore.preferences.core.stringPreferencesKey("active_list_id")
            ]
            if (activeListId == null) {
                startActivity(Intent(this@QuickAddActivity, MainActivity::class.java))
                finish()
                return@launch
            }
            val location = resolveLocation(name)
            addItemUseCase(
                ShoppingItem(
                    name = name,
                    location = location,
                    type = ItemType.RECURRING,
                    addedBy = userId,
                    addedByName = displayName,
                    listId = activeListId
                )
            )
            itemRepository.saveHistory(name, location, "", "")
            WidgetUpdater.update(applicationContext)
            finish()
        }
    }

    /** סיווג מקומי בלבד: hints → history → ברירת מחדל. ללא קריאת רשת (Gemini). */
    private suspend fun resolveLocation(name: String): ShoppingLocation {
        ItemNotePresets.suggestLocation(name)?.let { return it }
        itemRepository.getHistory(name)?.location?.let { return it }
        return ShoppingLocation.SUPERMARKET
    }
}
```

- [ ] **Step 3: רשום את ה-Activity ב-Manifest**

ב-`app/src/main/AndroidManifest.xml`, בתוך `<application>`, אחרי ה-`<receiver>` של ה-widget:

```xml
<activity
    android:name=".presentation.widget.QuickAddActivity"
    android:exported="false"
    android:excludeFromRecents="true"
    android:theme="@style/Theme.BuySmart.Transparent" />
```

- [ ] **Step 4: ודא קומפילציה**

Run: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && cd c:/Projects/buy_smart && ./gradlew.bat :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 5: Commit**

```bash
git add app/src/main/java/com/amir/buysmart/presentation/widget/QuickAddActivity.kt app/src/main/res/values/themes.xml app/src/main/AndroidManifest.xml
git commit -m "feat: QuickAddActivity - דיאלוג הוספה מהירה מה-widget"
```

---

## Task 8: hook לעדכון ה-widget מתוך `ItemRepositoryImpl`

ה-widget מתעדכן בכל שינוי נתונים: snapshot מ-Firestore, הוספה, מחיקה, ועדכון.

**Files:**
- Modify: `app/src/main/java/com/amir/buysmart/data/repository/ItemRepositoryImpl.kt`

- [ ] **Step 1: הזרק Context לבנאי**

החלף את הבנאי הקיים (שורות 17-21):

```kotlin
class ItemRepositoryImpl @Inject constructor(
    private val firestoreService: FirestoreService,
    private val itemDao: ItemDao,
    private val historyDao: ItemHistoryDao,
    @dagger.hilt.android.qualifiers.ApplicationContext private val context: android.content.Context
) : ItemRepository {
```

- [ ] **Step 2: עדכן את ה-widget אחרי כל שינוי נתונים**

ב-`getItemsForList`, בתוך ה-`onEach`, אחרי `itemDao.replaceItemsForList(...)`:

```kotlin
override fun getItemsForList(listId: String): Flow<List<ShoppingItem>> {
    return firestoreService.getItemsForList(listId).onEach { items ->
        itemDao.replaceItemsForList(listId, items.map { ShoppingItemEntity.fromDomain(it) })
        com.amir.buysmart.presentation.widget.WidgetUpdater.update(context)
    }
}
```

ב-`addItem`, אחרי `firestoreService.addItem(item)`:

```kotlin
override suspend fun addItem(item: ShoppingItem) {
    firestoreService.addItem(item)
    com.amir.buysmart.presentation.widget.WidgetUpdater.update(context)
}
```

הערה: `addItem` כותב רק ל-Firestore; ה-snapshot listener (`getItemsForList`) ירענן את Room ואת ה-widget ממילא כשהשרת מאשר. הקריאה כאן נותנת עדכון מיידי גם כש-listener לא פעיל (למשל הוספה מ-QuickAddActivity). שתי הקריאות אידמפוטנטיות.

- [ ] **Step 3: ודא build מלא עובר**

Run: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && cd c:/Projects/buy_smart && ./gradlew.bat :app:assembleDebug`
Expected: BUILD SUCCESSFUL.

- [ ] **Step 4: הרץ את כל בדיקות היחידה**

Run: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && cd c:/Projects/buy_smart && ./gradlew.bat :app:testDebugUnitTest`
Expected: BUILD SUCCESSFUL - כל הבדיקות עוברות (כולל WidgetItemsTest).

- [ ] **Step 5: Commit**

```bash
git add app/src/main/java/com/amir/buysmart/data/repository/ItemRepositoryImpl.kt
git commit -m "feat: עדכון ה-widget בכל שינוי נתונים ב-ItemRepositoryImpl"
```

---

## Task 9: בדיקה ידנית סופית

**Files:** אין - בדיקה ידנית במכשיר/אמולטור.

- [ ] **Step 1: התקן את ה-APK**

Run: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && cd c:/Projects/buy_smart && ./gradlew.bat :app:assembleDebug && adb install -r app/build/outputs/apk/debug/app-debug.apk`
Expected: `Success`.

- [ ] **Step 2: בדיקות ידניות (סמן כל אחת)**

- [ ] הוסף את ה-widget למסך הבית (לחיצה ארוכה על מסך הבית → Widgets → BuySmart). ה-widget מופיע עם הכותרת וכפתור +.
- [ ] ה-widget מציג את פריטי הרשימה הפעילה שטרם נקנו, עם אימוג'י מיקום.
- [ ] לחיצה על כפתור + פותחת את דיאלוג ההוספה הצף (בלי לפתוח את האפליקציה המלאה). הוספת פריט → הדיאלוג נסגר וה-widget מציג את הפריט החדש.
- [ ] לחיצה על פריט / על הכותרת פותחת את האפליקציה במסך הבית.
- [ ] סימון פריט כנקנה באפליקציה → הפריט נעלם מה-widget (לאחר רגע).
- [ ] רשימה ריקה → מוצג "הרשימה ריקה 🎉".

- [ ] **Step 3: עדכן SPEC + memory**

- ב-`SPEC.md`: סמן את שורת ה-Widget כ-`✅` תחת "🟡 MEDIUM — שיפורים UX".
- צור memory חדש `feature_widget.md` עם פרטי המימוש + עדכן את `MEMORY.md`.

- [ ] **Step 4: Commit סופי**

```bash
git add SPEC.md
git commit -m "docs: Widget למסך הבית הושלם - עדכון SPEC"
```

---

## Self-Review notes (למבצע)

- **כיסוי spec:** כל סעיף בעיצוב ממופה למשימה - תצוגה (Task 5), הוספה מהירה (Task 7), עדכון (Task 8), קצוות (Task 5 loggedOut/empty + Task 7 ללא משתמש), בדיקות (Task 2 + Task 9).
- **תלות בין משימות:** `WidgetUpdater` (Task 4) תלוי ב-`BuySmartWidget` (Task 5) - לכן הם נבנים ומתקמפלים יחד. בצע לפי הסדר.
- **שמות עקביים:** `widgetItems(items, max)`, `WidgetUpdater.update(context)`, `WidgetEntryPoint.itemDao()/dataStore()`, מפתח DataStore `"active_list_id"` (זהה ל-`ListRepositoryImpl`).
- **אימות גרסת Glance:** אם imports של Glance נכשלים בקומפילציה, ייתכן שינוי קל בשמות חבילה - הסתמך על השלמות ה-IDE/compiler; ה-API הציבורי יציב ב-1.1.1.
